@model IEnumerable<SkiStatsAppV2.Models.Sortie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CentreDeSki.NomDuCentre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Saison.SaisonId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CentreDeSki.NomDuCentre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Saison.SaisonId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SortieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SortieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SortieId })
        </td>
    </tr>
}

</table>
